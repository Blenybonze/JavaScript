
    //Variável hoje recebe valor primitivo do JS, que é verificar a data
    var hoje = new Date();

    //Usando a propriedade getDay podemos receber o valor do dia correspondente de domingo = 0 até sabado igual a 6
    hoje.getDay();

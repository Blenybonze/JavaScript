/*a principio uma versão mais simplificada de escrever funções
usarei como exemplo um trecho de código avulso
Exemplo escrito como antes:*/
var exemplo = exemplo2.some(function(nota){
    return nota === 0;
});

/*Exemplo escrito novamente:*/
var exemplo = exemplo2.some( nota => nota === 0);
// Verificando valores dentro de um Array

const cores= [
  "verde",
  "amarelo",
  "azul",
  "branco"
];

let index = 0;

while(index < cores.length){
  console.log('A cor é '+cores[index]);
  index++;
}
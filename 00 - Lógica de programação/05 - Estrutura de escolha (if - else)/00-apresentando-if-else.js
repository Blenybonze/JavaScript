//APRESENTANDO O IF ELSE

    var n1 = parseFloat ( prompt("Numero um numero: "));

    var n2 = parseFloat ( prompt("Numero um numero: "));


    var media = (n1 + n2)/2; //criando variável que faz o cálculo

    if(media > 5){ //Se a média for maior que 5
        alert("Parabéns! Você foi aprovado")//Este texto será mostrado
    } 
    else{ //Se não for, mostrata o alert abaixo
        alert("Reporvado, estude mais na próxima vez!")
    }

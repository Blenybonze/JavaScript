var nome = prompt("Qual seu nome?"); /*Armazenando uma variável na memória da maquina do usuário(var=variável / nome=nome da variável criada / prompt=o que aparecera na tela para o usuário)*/

alert("Bem vindo, " + nome); /*Criando um alert que utiliza a variável criada (Texto sempre dentro de "duas aspas" e fora o sinal de + juntamente do nome da variável)*/